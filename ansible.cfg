[defaults]

forks = 40
timeout = 20
roles_path = ./roles
fact_caching_timeout = 7200
log_path = ansible.log
host_key_checking = False
remote_user = ubuntu

[privilege_escalation]

become = True
become_method = sudo
become_user = root
